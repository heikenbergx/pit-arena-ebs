{
  "name": "pit-arena-ebs",
  "version": "0.1.0",
  "private": true,
  "description": "Pit Arena Twitch Extension helper server (EBS) — holds every player's gear/stash so the panel can show it.",
  "type": "commonjs",
  "main": "index.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
